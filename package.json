{
  "name": "caseno4",
  "private": true,
  "description": "Monorepo root for Vercel — web app lives in apps/web",
  "scripts": {
    "build": "npm run build --prefix apps/web",
    "dev": "npm run dev --prefix apps/web",
    "preview": "npm run preview --prefix apps/web"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@vercel/node": "^5.9.3"
  }
}
