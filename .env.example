DATABASE_URL=postgresql+asyncpg://vyhod:vyhod@db:5432/vyhod
JWT_SECRET=change-me-in-production
AI_PROVIDER=fake
OPENAI_API_KEY=
CORS_ORIGINS=http://localhost:3000

# xAI / SpaceXAI (Grok) — server-side only. Used by /api/assistant (Vercel + local Vite).
# Create at https://console.x.ai — never commit a real key.
XAI_API_KEY=

