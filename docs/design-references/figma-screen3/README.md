# Screen 3

This is a code bundle for Screen 3. It's based on default Vite project. The original design is available at https://app.flowstep.ai/file?activeFileId=7f735cb4-8df5-4318-89fd-954691efdbb7.

## Running the code

Run `npm i` and `npm run setup` to install the dependencies.

Run `npm run dev` to start the development server.
Figma Screen 3 source adapted into apps/web Home (Today.tsx + mint.css).
